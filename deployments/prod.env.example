# This config replicates ../.env.example
# However, this file is used by systemd service in production deployment
# Hence you can't use that "export VAR=VALUE syntax"
# Instead, use "VAR=VALUE" syntax